/*
Package engines provides the general CLI flags for dealing with container
engines in general, such as disabling them.

  - --noengines disables consulting any container engine.
  - --keep-going skips failed container engines.
*/
package engines
